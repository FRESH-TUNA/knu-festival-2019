# SECRET_KEY
SECRET_KEY=${SECRET_KEY}

# AWS Setting
AWS_REGION=ap-northeast-2
AWS_STORAGE_BUCKET_NAME=knufesta2019

DB_NAME=${DB_NAME}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
ALLOWED_HOSTS=${ALLOWED_HOSTS}
